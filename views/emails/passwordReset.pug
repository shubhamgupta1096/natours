extends baseEmail

block content
  p Hi #{firstName},
  p Forgot your password? Send a patch request with the new password and the confirmPassword to: #{url}.
  p (Website for this template not yet implemented.)
  table.btn.btn-primary(role='presentation', border='0', cellpadding='0', cellspacing='0')
    tbody
      tr
        td(align='left')
          table(role='presentation', border='0', cellpadding='0', cellspacing='0')
            tbody
              tr
                td
                  a(href=`${url}`, target='_blank') Reset your Password.
  p If you didn't forgot your password then please ignore this email!